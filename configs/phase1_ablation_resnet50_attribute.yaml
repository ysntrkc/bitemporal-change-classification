# A1 ablation: same canonical recipe with ResNet-50 backbone.
experiment:
  phase: 1
  name: phase1_attribute_resnet50
  family: attribute
  n_classes: 24
  output_dir: results/phase1_attribute_resnet50/seed42
model:
  backbone: resnet50.a1_in1k
  droppath: 0.0
