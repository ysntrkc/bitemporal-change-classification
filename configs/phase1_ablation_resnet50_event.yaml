# A1 ablation: same canonical recipe with ResNet-50 backbone.
experiment:
  phase: 1
  name: phase1_event_resnet50
  family: event
  n_classes: 12
  output_dir: results/phase1_event_resnet50/seed42
model:
  backbone: resnet50.a1_in1k
  droppath: 0.0
